# Copy this file to .env and set a real password before running
# `docker compose up`. Used by both the `web`/`cron` app containers (as
# MYSQL_PASSWORD) and the `db` container (as MARIADB_ROOT_PASSWORD)
DB_ROOT_PASSWORD=changeme
