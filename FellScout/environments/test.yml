# configuration file for the test environment (DANCER_ENVIRONMENT=test)

# log to the console rather than development.yml's default file path, which
# only exists on the primary developer's machine
logger: "console"
log: "warning"

warnings: 1
show_errors: 1
startup_info: 0
